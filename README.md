# Android-WebSocketProxy
A websocket proxy running on an Android device

It's using https://github.com/TooTallNate/Java-WebSocket under the hood to create both the websocket server and the websocket client.
